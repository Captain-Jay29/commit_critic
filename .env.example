# Commit Critic Configuration
# Copy this file to .env and fill in your values

# Required: OpenAI API Key (get from https://platform.openai.com/api-keys)
OPENAI_API_KEY=sk-your-api-key-here

# Optional: Override default model (default: gpt-5.2)
# OPENAI_MODEL=gpt-5.2

# Optional: Override embedding model (default: text-embedding-3-small)
# OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# Optional: Custom data directory (default: ~/.commit-critic)
# COMMIT_CRITIC_DATA_DIR=/path/to/data
